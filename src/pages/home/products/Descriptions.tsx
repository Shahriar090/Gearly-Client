const Descriptions = ({ description }: { description: string }) => {
  return (
    <div>
      <article>
        <p>{description}</p>
      </article>
    </div>
  );
};

export default Descriptions;
